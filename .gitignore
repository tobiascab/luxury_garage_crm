node_modules/
dist/
.env
.arizar-oauth-tokens.json
*.log
.DS_Store
package-lock.json

# Herramientas locales / artefactos (no van al repo)
.opencode/
.playwright-mcp/
.claude/skills/
dev-dist/

# Volcados de base de datos: nunca al repo (contienen datos de clientes y hashes)
*.sql
backup_*
# ...pero las migraciones SÍ van: son la estructura de las tablas, no datos de nadie.
!backend/prisma/sql/*.sql
